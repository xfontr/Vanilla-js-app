/**
 * For development purposes only.
 * Can switch the mode between 'open' and 'close'
 * It must be set at 'close' for production environments *
 */

const shadowOptions: ShadowRootInit = {
  mode: "open",
};

export default shadowOptions;
