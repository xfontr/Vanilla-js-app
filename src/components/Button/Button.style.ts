const buttonStyle = `
  button {
    cursor: pointer;
    user-select: none;
    font: inherit;
    outline: none;
    border: none;
  }

  .button {
    background-color: red;
  }
`;

export default buttonStyle;
